import React from "react";
import ProfilePage from "../../ProfilePage";

// Admin profile page – currently reuses the shared profile implementation.
// Split this out later if admin needs additional fields or layout.
const AdminProfilePage: React.FC = () => {
  return <ProfilePage />;
};

export default AdminProfilePage;






