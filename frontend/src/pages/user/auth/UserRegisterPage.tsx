import React from "react";
import RegisterPage from "../../RegisterPage";

// User-specific wrapper around the shared registration page.
const UserRegisterPage: React.FC = () => {
  return <RegisterPage />;
};

export default UserRegisterPage;






